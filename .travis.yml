language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ramlang' ]; then cd .. && eval "mv $currentfolder generator-ramlang" && cd generator-ramlang; fi

